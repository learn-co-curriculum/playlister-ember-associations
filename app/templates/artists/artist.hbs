<h4>{{model.name}}</h4>

albums:
<ul>
  {{#each model.albums as |album|}}
    <li class="albums">
      {{#link-to 'albums.album' album.id}}
        {{album.name}}
      {{/link-to}}
    </li>
  {{/each}}
</ul>

